# Kong Gateway Configuration

# Kong version
KONG_VERSION=3.13.0.0

# Database mode (should always be 'off' for read-only mode)
KONG_DATABASE=off

# Declarative configuration file
KONG_DECLARATIVE_CONFIG=/kong/declarative/kong.yml

# Log level (debug, info, notice, warn, error, crit)
KONG_LOG_LEVEL=info

# Proxy ports
KONG_PROXY_HTTP_PORT=8000
KONG_PROXY_HTTPS_PORT=8443

# Admin API ports
KONG_ADMIN_HTTP_PORT=8001
KONG_ADMIN_HTTPS_PORT=8444
KONG_ADMIN_GUI_PORT=8002

# Backend Services Configuration
# These should match the ports your actual services are running on

# Python Calculator MCP Server
PY_CALCULATOR_HOST=host.docker.internal
PY_CALCULATOR_PORT=8100

# Go Calculator MCP Server
GO_CALCULATOR_HOST=host.docker.internal
GO_CALCULATOR_PORT=8200

# Arize Phoenix Observability
PHOENIX_HOST=host.docker.internal
PHOENIX_PORT=6006
PHOENIX_GRPC_PORT=4317

# Ollama LLM Server
OLLAMA_HOST=host.docker.internal
OLLAMA_PORT=11434

# Resource limits
KONG_CPU_LIMIT=2.0
KONG_MEMORY_LIMIT=1G
KONG_CPU_RESERVATION=0.5
KONG_MEMORY_RESERVATION=512M
